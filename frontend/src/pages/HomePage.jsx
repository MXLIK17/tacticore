function HomePage({ mode, onModeChange, onStart }) { return <div className="home-page"><section className="home-hero"><span className="eyebrow">The greatest XI is waiting</span><h1>Build football<br /><em>your way.</em></h1><p>Draft legendary players from history. Set your tactics. Find out whether your XI can stand with the greats.</p><div className="mode-options">{[["premier", "Premier League Mode", "Historic English clubs", "38-match season"], ["worldcup", "FIFA World Cup Mode", "Legendary national teams", "Knockout glory"]].map(([id, title, copy, format]) => <button key={id} className={mode === id ? "mode-option is-selected" : "mode-option"} onClick={() => onModeChange(id)}><span className="mode-index">{id === "premier" ? "01" : "02"}</span><strong>{title}</strong><span>{copy}</span><small>{format}</small></button>)}</div><button className="primary-button start-button" onClick={onStart}>Enter the draft <span>→</span></button><div className="home-footnote"><span>11 players</span><i /> <span>One legacy</span><i /> <span>Your tactics</span></div></section><div className="home-pitch-art" aria-hidden="true"><span /><span /><span /><b>TC</b></div></div>; }
export default HomePage;
