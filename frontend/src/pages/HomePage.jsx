function HomePage({ mode, onModeChange, onStart }) { return <div className="home-page"><section className="home-hero"><span className="eyebrow">Football history, your tactics</span><h1>Build the XI<br />history never had.</h1><p>Draft iconic players from football’s greatest teams. Shape the lineup. Put it to the test.</p><div className="mode-options">{[["premier", "Premier League", "Historic English clubs"], ["worldcup", "World Cup", "Legendary national teams"]].map(([id, title, copy]) => <button key={id} className={mode === id ? "mode-option is-selected" : "mode-option"} onClick={() => onModeChange(id)}><strong>{title}</strong><span>{copy}</span></button>)}</div><button className="primary-button" onClick={onStart}>Build your squad <span>→</span></button></section><div className="home-pitch-art" aria-hidden="true"><span /><span /><span /></div></div>; }
export default HomePage;
