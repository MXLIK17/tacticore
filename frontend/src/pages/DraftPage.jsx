import DraftOffer from "../components/draft/DraftOffer";
import FootballPitch from "../components/squad/FootballPitch";
import DraftProgress from "../components/draft/DraftProgress";

function DraftPage({ formation, slots, players, offer, loading, error, spin, select, onBack }) { const filled = Object.keys(players).length; const progressSlots = slots.map((slot) => ({ ...slot, filled: Boolean(players[slot.id]) })); return <div className="draft-page page-wrap"><section className="page-heading compact"><div><span className="eyebrow">Squad draft · {formation}</span><h1>Make the call.</h1></div><button className="secondary-button" onClick={onBack}>View squad</button></section><DraftProgress filled={filled} slots={progressSlots} /><div className="draft-layout"><FootballPitch slots={slots} players={players} onSlotClick={spin} /><div className="draft-decision"><div className="decision-caption"><span>Next decision</span><b>{offer?.position || "Choose a position"}</b></div>{loading && <p className="loading-message">Searching football history…</p>}{error && <p className="error-message">{error}</p>}<DraftOffer offer={offer} loading={loading} onSelect={select} onDismiss={onBack} /></div></div></div>; }
export default DraftPage;
