function ResultsPage({ mode, seasonResult, worldCup, onBuildAgain }) { const isWorldCup = mode === "worldcup"; const ready = isWorldCup ? worldCup?.finished : seasonResult; return <div className="results-page page-wrap"><span className="eyebrow">Competition results</span>{!ready ? <section className="results-empty"><h1>No final whistle yet.</h1><p>Take your completed squad into a competition to create its story.</p></section> : isWorldCup ? <section className="results-summary"><p className="result-kicker">World Cup champions</p><h1>{worldCup.champion}</h1><div className="result-list">{worldCup.history.map((match) => <div key={`${match.stage}-${match.teamB}`}><span>{match.stage}</span><strong>{match.teamA} <b>{match.score}</b> {match.teamB}</strong><small>{match.winner} won</small></div>)}</div></section> : <section className="results-summary"><p className="result-kicker">Premier League campaign</p><h1>{seasonResult.points} points</h1><div className="result-stats"><span><strong>{seasonResult.wins}</strong>Wins</span><span><strong>{seasonResult.draws}</strong>Draws</span><span><strong>{seasonResult.losses}</strong>Losses</span><span><strong>{seasonResult.goalsFor}–{seasonResult.goalsAgainst}</strong>Goals</span></div><p>Top scorer: <strong>{seasonResult.topScorer?.name || "—"}</strong></p><p>Top assister: <strong>{seasonResult.topAssister?.name || "—"}</strong></p></section>}<button className="primary-button" onClick={onBuildAgain}>Return to squad</button></div>; }
export default ResultsPage;
